NEXT_PUBLIC_GOOGLE_CLIENT_ID=217184380827-ahiqdrhb1cp9cc1mvfa60o8rkvjqb0d9.apps.googleusercontent.com
# NOTE: Server-side secrets (JWT_SECRET, GOOGLE_CLIENT_SECRET, etc.) must NOT be placed in a frontend `.env.local`.
# Move those values to the server-side environment (for example: `server/.env` or your process manager) so they are not shipped to clients.
# If you need a redirect URI value for the server OAuth flow, put it in the server's env (not here).